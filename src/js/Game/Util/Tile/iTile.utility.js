module.exports =
class iTileUtility {

    /**
     * Generate a tileset onto the given scene, apply to the given group,
     * and apply the data provided in its creation.
     *
     * @param {Phaser.Scene} scene
     * @param {Phaser.GameObjects.Group} group
     * @param {} data
     */
    static generate(scene, group, data) { throw "method not implemented"; }
}